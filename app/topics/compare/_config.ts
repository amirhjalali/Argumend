import { buildGenericOgUrl } from "@/lib/og";

export const COMPARE_INDEX_SOCIAL_IMAGE = buildGenericOgUrl({
  title: "Compare Topics Side by Side",
  subtitle: "Evidence, argument pillars, and cruxes for any two debates",
});
